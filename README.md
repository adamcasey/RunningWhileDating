## Installs
Need to run:  
&nbsp;&nbsp;&nbsp;&nbsp;`npm install`  
&nbsp;&nbsp;&nbsp;&nbsp;`npm install nodemon`  

## Notes
Once installs are done, use `npm run dev` from the command line to start the app  
